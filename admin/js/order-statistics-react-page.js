(function () {
  "use strict";

  // Safe placeholder kept so the shared admin footer does not load a missing file.
  // The product add/edit React experience is handled in product-react-form.js.
})();
